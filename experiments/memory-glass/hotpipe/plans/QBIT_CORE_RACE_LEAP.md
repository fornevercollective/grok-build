# Next leap · Quantum → Core Race · Collab Truss · Native μs

> **Status 2026-07-19T05:55Z:** leap **foundation LIVE** (smoke + cargo path).  
> Spine fabric green. Velocity layer = native IPC stamp · truss · core-race ladder.  
> Honest: not true sub-μ in WK JS. Next days = seat registry · human-gated monetize · Rust loop.  
> Role: leap lane owns native/truss; chrome peer stays off this file for thrash.


**Status:** foundation shipping · 2026-07-18  
**North star:** Memory Glass + kbatch.ugrad.ai + mueee core-race as one **fleet OS** that keeps working while machines would otherwise sleep.

Live target: [qbit-core-race](https://mueee.qbitos.ai/qbit-core-race.html) · [kbatch](https://kbatch.ugrad.ai/) · MG operator console

---

## Why this leap

Spine (bus · dual-clock · adapters) is the **routing fabric**.  
The leap is **velocity + scale**:

| Layer | What you have | What the leap adds |
|-------|----------------|--------------------|
| Encode | QbitCodec · steno · gutter | IronLine provenance on **every** hop |
| Runtime | JS L3 μs-class | **Native IPC** μs stamp + later true sub-μ |
| Collab | mesh · collab-day | **Truss**: claims · personas · handoffs · dormant jobs |
| Product | MG lab shell | Climb ladder to **core-race** + kbatch live iteration |
| Economy | local train / WebGrid | Dormant-machine **churn** that can monetize (human-gated) |

This is **Grokpool-level multi-agent presence** on a **Dojo/Colossus-shaped** work graph — not chat tabs.

---

## Architecture (leap)

```
                 ┌──────────── agents / personas / panda panes ────────────┐
                 │  claim · handoff · dormant job · race climb              │
                 └───────────────────────┬─────────────────────────────────┘
                                         │
                    ┌────────────────────▼────────────────────┐
                    │         __mgQbitTruss  (JS)             │
                    │   + __mgQbitBus envelopes               │
                    └───────────┬───────────────┬─────────────┘
                                │               │
              ┌─────────────────▼──┐     ┌──────▼──────────────┐
              │ IronLine L0–L7     │     │ __mgQbitNative      │
              │ every hop          │     │ IPC op: qbit_env    │
              └────────────────────┘     │ nativeTμ stamp      │
                                         └──────┬──────────────┘
                                                │ main.rs
                                         main ◄─┴─► inspect
                                         (board_live class channel)

   sensors ─► bus ─► loop (L3) / cortical L5 ─► adapters ─► kbatch / WebGrid / MKT
                                         │
                                         ▼
                              core-race · dojo · gutter · tensor
```

---

## Climb ladder (rungs)

| # | Rung | Lane | Meaning |
|---|------|------|---------|
| 0 | L3-quantum | L3 | classify / encode / glyph |
| 1 | L0-speed | L0 | ingest / native stamp |
| 2 | L5-render | L5 | never thrash paint |
| 3 | L2-commander | L2 | agent dispatch / claim |
| 4 | L7-persona | L7 | persona adopt / voice intent |
| 5 | **core-race** | L0 | mueee qbit-core-race · fleet sitrep |

APIs:

```js
__mgQbitTruss.adoptPersona({ name, goal, monetize, kbatch: true })
__mgQbitTruss.claim({ title, files, tests, monetize })
__mgQbitTruss.handoff({ to, summary, prompt, files })
__mgQbitTruss.climb("why")
__mgQbitTruss.enqueueDormant({ kind: "encode"|"classify"|"gutter", monetize })
__mgQbitTruss.openCoreRace()
__mgQbitNative.report()   // IPC mirror stats
```

---

## Native path (honest)

| Now | Later |
|-----|--------|
| `op: "qbit_env"` main↔inspect | Dedicated Rust qbit_loop thread |
| `nativeTμ` wall-clock μs | TSC / mach_absolute_time sub-μ |
| JS L3 50–500 μs classify | WASM or Rust codec for long docs |
| BroadcastChannel mesh | Same envelope over LAN / Colossus bus |

**Sub-μ truth:** browser JS will not be a hard RTOS. Native + offline μgrad is where ns/μs gates live. The leap **wires the escape hatch** and stamps every cross-pane hop.

---

## Collaborative truss (Grokpool-shaped)

| Primitive | Role |
|-----------|------|
| **Persona** | Who is working (overnight agent identity) |
| **Claim** | Exclusive slice (TTL) so agents don’t thrash same file |
| **Handoff** | panda α→β→γ pack on the bus |
| **Dormant job** | Work queue when host would idle |
| **Race event** | Climb signal toward core-race |

Conflict rule (unchanged): **one alphabet** — only one agent edits `QbitCodec.SYMBOLS`.

---

## Ship checklist

### Foundation (this cook)

- [x] JS spine v2/v3 (bus · loop · router · adapters · gutter)
- [x] `Cmd::QbitEnv` + IPC `qbit_env` in `main.rs`
- [x] `qbit-native-bridge.js` mirror
- [x] `qbit-truss.js` personas/claims/handoffs/dormant/climb
- [x] Plan doc (this file)

### Next (days)

- [ ] Inject native+truss in lean path + hotpipe bake
- [ ] Wire truss handoff → `~/.panda/lab-handoff.json` writer
- [ ] IronLine auto-report strip on inspect HUD
- [ ] kbatch dojo phrase → truss climb when strain band improves
- [ ] Cargo build / resign for native path (requires rebuild, not only ⌘⇧R)

### Quarter (Dojo / Colossus scale)

- [ ] Fleet seat registry (mini · laptop · cloud worker)
- [ ] Monetize hooks human-gated (no silent spend)
- [ ] Core-race live SITREP feed from MG metrics
- [ ] Deprecate direct `__mgX` cross-calls behind bus

---

## Morning / overnight goals (updated)

1. Spine + leap foundation green in smoke  
2. Live MG: truss report + native report after rebuild  
3. Peer keeps menu/chrome; this lane owns **native+truss+race**  
4. Disk stay soft (no full soak)  

---

## Related

- `plans/QBIT_BUS_HANDOFF.md` — spine contract  
- `plans/IRONLINE_CORTICAL.md` — L0–L7 budgets  
- `docs/MEMORY-GLASS-GROWTH.md` — product pillars  
- mueee: `/qbit-core-race.html` · `/quantum-gutter.html` · `/ugrad-r0.html`  
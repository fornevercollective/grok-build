# Month of collaboration · Memory Glass as the machine

**Feel:** brand-new approach — not bolt-on adapters.  
**Open a machine → multi-tier chat command lives *inside* the browser OS → AI tests, breaks, QA, gets curious.**

---

## What “from scratch tomorrow” means

You are not starting code from zero. You are starting **operations** from a clean story:

| Layer | What it is |
|-------|------------|
| **Fleet map** | `FLEET.md` + catalogue (where everything lives) |
| **OS fabric** | qbit bus · dual-clock · IronLine · adapters |
| **Velocity** | native `qbit_env` · truss · dormant · L1 term |
| **Language plane** | kbatch R4 · staff · dojo · living books |
| **Command UX** | **Agent Desk** αβγδ inside MG |
| **Fix loop** | term-snap → Grok → ship |
| **Peer lane** | other grok-build terminal |

**Stocks Monday + build month:** MG is the **live stream + lab shell**; kbatch is content; truss keeps agents from thrashing the same tree.

---

## Multi-tier command (Cursor-shaped, SpaceX-owned energy)

Inside Memory Glass (main pane):

| Tier | Glyph | Job |
|------|-------|-----|
| **α PLAN** | plan | intent · claim product · handoff |
| **β BUILD** | build | notes → truss handoff · open tools |
| **γ VERIFY** | verify | QA spine · smoke selfTests · break tests |
| **δ EXPLORE** | explore | curious “what if…” · race · kbatch |

UI: floating **DESK αβγδ** (bottom-left) → `__mgAgentDesk`

```js
__mgAgentDesk.open()
__mgAgentDesk.run("/claim kbatch")
__mgAgentDesk.run("/curious")
__mgAgentDesk.run("/qa")
__mgAgentDesk.run("/peer")   // handoff to other Grok terminal
```

The AI (you + Grok terminals + desk) should feel free to:

1. **Test** — `/smoke` `/qa`  
2. **Move** — `/nav` `/tools` `/kbatch` `/race`  
3. **Break** — `/break` (bus backpressure)  
4. **Get curious** — `/curious`  
5. **Collaborate** — `/peer` · truss claim different seats  

---

## Two Grok terminals · collab protocol

| Seat | Owns | Does not |
|------|------|----------|
| **Leap / this** (`019f78be`) | native · truss · desk · dormant · race · L1 pilot | bulk reorg · kbatch SPA content thrash |
| **Peer** (`019f6e76` / fleet) | catalogue · term-snap · chrome · kbatch content · Cloudflare | SYMBOLS · fake sub-μ |

**Daily join:**

```text
Peer:  tsnap chrome bugs · kbatch living-books/dojo content · FLEET scan
Leap:  Agent Desk live · uvspeed publishBus pilot · H5 SITREP · stocks prep board later
Both:  /peer handoff or lab-handoff.json · no SYMBOLS fork
```

**Shared truth files:**

- `FLEET.md`
- `hotpipe/plans/OS_FLIGHT_PATH.md`
- `hotpipe/plans/MONTH_COLLAB.md` (this)
- `~/.panda/mg-soak/STEER.md`
- `~/.panda/lab-handoff.json`

---

## Week-by-week (one month)

| Week | Theme | Output |
|------|--------|--------|
| **W1** | Live desk + native verify + kbatch claim | Streamable “open MG → DESK → claim → curious” |
| **W2** | L1 terminal pilot (uvspeed) + staff/gutter on bus | One terminal writes envelopes |
| **W3** | H5 core-race SITREP + dormant overnight jobs | Livestream meters that mean something |
| **W4** | Stocks/MKT human-gated + multi-seat collab day | Peer + leap same board without thrash |

**Monday stocks:** MKT filmstrip stays human-gated; desk `/claim` does not auto-trade. Use verify tier before any live risk.

---

## Livestream checklist (ready)

1. `open ~/Applications/Memory Glass.app`  
2. ⌘⇧R · open **DESK αβγδ**  
3. `/persona stream-agent` · `/claim memory-glass` · `/claim kbatch` (second agent)  
4. `/qa` · `/curious` · `/race`  
5. Chrome glitch → `tsnap` → paste into Grok on stream  
6. Optional: dormant worker in second pane  

---

## Organizing “everything in my brain on this machine”

| Want | Where |
|------|--------|
| What exists | catalogue / AWESOME |
| How OS works | OS_FLIGHT_PATH + QBIT_CORE_RACE_LEAP |
| How we work together | **this file** |
| How to fix UI fast | term-snap |
| How to command AI inside browser | Agent Desk |
| Language/schools/staff | KBatch-dictionary |
| Operator console | Memory Glass |

**Do not** bulk-move trees this month. **Do** claim seats and ship on the bus.

---

## Success (end of month)

Opening any cold machine on the fleet, you can:

1. Read `FLEET.md` in 60s  
2. Launch MG + Agent Desk  
3. Spin αβγδ agents (human + Grok + peer) without path fights  
4. Stream learning (kbatch + WebGrid + race) while dormant jobs run  
5. Still have room for Monday markets without OS thrash  

That’s the brand-new approach: **the browser is the OS; the desk is the IDE; the truss is multiplayer; the catalogue is the map.**

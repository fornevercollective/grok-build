/target
**/*.app
.DS_Store
*.log

# build / app bundle (not source)
/target/
/*.app/
/Memory Glass.app/
BUILD_STAMP
hotpipe/out/
hotpipe/patches/
# full board lives in ~/.panda; keep only slim sample in hotpipe/data if present
# (committed sample is intentionally small)
icons/app-icon-xai-alt.jpg

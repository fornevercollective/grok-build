# /web · multi-browser inspect onramp

```bash
fcs web [inspect|browsers|hygiene|learn|pack|soak|onramps|open <browser>]
fcs inspect
fcs hygiene
```

Opens Memory Glass job-hygiene + `/web inspect` panel via bus  
`~/.panda/mg-session/web-cmd.json`.

Peer browsers: Safari · Chrome · Firefox · Edge · Arc · Orion · Brave.

---
description: Glyph engine — arena peel, broadcast TX/RX, live translation, webgrid
argument-hint: "[arena|peel|popout|broadcast|encode|decode|translate|webgrid|stack|doctor]"
---

Run (do not reimplement):

```bash
fcs glyph $ARGUMENTS
```

Defaults:

```bash
fcs glyph
fcs glyph popout
fcs glyph translate from en to es,fr,ja
fcs glyph broadcast bloomberg
fcs glyph stack
```

Surfaces: http://127.0.0.1:8790/ugrad-arena.html?mode=glyph · lang-chat-desk · webgrid-ugrad · llms-glyph.txt

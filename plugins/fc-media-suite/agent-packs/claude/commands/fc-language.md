# /language · multi-stream keyboard translation

Simultaneous keyboard streams (layout · translate · codec). Prefer shell:

```bash
fcs language
fcs language popout
```

In Grok TUI: `/language` · `/language translate` · keys Tab / Ctrl+m / Esc.

---
description: fornevercollective · /web inspect via universal fcs CLI (any terminal / any AI)
---

Run (do not reimplement):

```bash
fcs web $ARGUMENTS
```

Subcommands: `inspect` · `browsers` · `hygiene` · `learn` · `pack` · `soak` · `onramps` · `open safari|chrome|firefox|…`

Aliases: `fcs inspect` · `fcs hygiene` · `fcs /web`

Install: `fcs install`. Skill: fc-media-suite · memory-glass.

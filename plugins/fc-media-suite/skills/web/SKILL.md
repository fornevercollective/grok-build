---
name: web
description: >
  /web inspect multi-browser DevTools onramp + Memory Glass job hygiene.
  Triggers: /web, /inspect, /hygiene, fcs web, zombie download, DevTools.
---

# /web inspect

Universal entry from **any terminal / any AI**:

```bash
fcs web
fcs web browsers --json
fcs web hygiene
fcs web open safari
fcs web onramps
```

In Memory Glass Grok term: `/web` · `/inspect` · `/hygiene`.

See `experiments/memory-glass/hotpipe/plans/FIELD_TRIGGER_LOOP.md`.

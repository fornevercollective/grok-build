# Stair · ugrad-r0 → tensor · SpaceX-tweetable wedge

**Goal:** Independent, incremental growth of **Memory Glass × Grok terminal** toward a capability **not on the market** — measure-first training glass, not chat browser.

**Live now:** MG launched on `https://mueee.qbitos.ai/ugrad-r0.html` · persona hub on `:8765` · KBatch CF live.

---

## Market gap (A–d+)

| What market has | What we ship |
|-----------------|--------------|
| Chat-first AI browsers | **Measure-first** BPS / strain / path |
| Generic agent tabs | **WK droplet + 1s hotpipe** lab |
| Cloud memory products | Local packs · soak · offline Ollama |
| Disconnected train sites | **One glass:** WebGrid · μgrad · KBatch · Cage |
| No epistemic filter | **Cage litmus** FACT / FICTION / STONE_TRAP |

**Wedge line:** *Native glass that turns WebGrid, language geometry, and μgrad into one instrument — Grok terminal drives the stair; humans keep the post button.*

---

## Stair (do in order — each step is demoable)

### S0 · Suit alive (done when green)
- [x] Launch Memory Glass  
- [x] Open **ugrad-r0** (μgrad terminal / tensor curriculum)  
- [x] Persona scaffold served (`:8765/persona-tensor-scaffold.html`)  
- [ ] Crash → auto relaunch (script below)

**Grok terminal:** `open MG · verify PID · curl ugrad-r0 200`

### S1 · Motor tensor (Letter-Grid)
- Open KBatch Letter-Grid `?v=pipe8` in MG  
- `kbatch_lettergrid_ping` → 6235 glyphs / 44 layers  
- Optional: pipe smoke URL  
- Score / Dojo step if human present  

**Artifact:** session note with peak BPS or smoke pass.

### S2 · Epistemic tensor (Cage)
- Cage litmus page or MCP  
- `kbatch_cage_litmus_verify` with honest labels  
- Prove hard rule (zero STONE_TRAP → fail)  

**Artifact:** grade JSON (oracle or live agent).

### S3 · Genealogical bind (Rubik)
- `kbatch_lettergrid_rubik` → Focus `pie-germanic-en`  
- Optional Shadow Live focus in DOJO  

**Artifact:** bind pack path + 13 pathways listed.

### S4 · Geometry / calibration (agent)
- Cost matrix lookup (same $c$ as DOJO — **not** tilde_c in UI)  
- JAX bank probe for `decl-head-40`  
- Offline: `MG_LOCAL_LLM=1` brief on pack  

**Artifact:** morning-brief.md or kernel rank sample.

### S5 · μgrad train loop (ugrad-r0)
- Seed train on R0 in glass  
- Record curriculum step (U0→U1 if available)  
- Link BPS day to μgrad day in leaderboard draft  

**Artifact:** train screenshot + one metric line.

### S6 · Grok terminal skill demo (tweet fuel)
Show Grok **orchestrating** the stair without human mouse theater:

1. Launch / relaunch MG if dead  
2. Hit ugrad-r0 + Letter-Grid + Cage MCP  
3. Write one evidence pack under `~/.panda/mg-session/`  
4. Produce X draft (**human posts**)

---

## Crash / iterate protocol (while exploring)

```bash
# status
pgrep -x memory-glass || echo dead

# relaunch tensor entry
pkill -x memory-glass; sleep 0.5
open -na "$HOME/Applications/Memory Glass.app" --args \
  "https://mueee.qbitos.ai/ugrad-r0.html"

# alternate stair URLs (one per relaunch)
# WebGrid:  https://neuralink.com/webgrid/
# Letter-Grid: https://kbatch.ugrad.ai/labs/declaration-digital-edition/letter-grid.html?v=pipe8
# Cage: https://kbatch.ugrad.ai/labs/declaration-digital-edition/cage-litmus.html
# Persona: http://127.0.0.1:8765/persona-tensor-scaffold.html
# KBatch DOJO: https://kbatch.ugrad.ai/dojo/

# after hotpipe edit
cd experiments/memory-glass && bash scripts/resign-app.sh

# short soak smoke
MG_LOCAL_LLM=1 bash scripts/overnight-soak.sh --hours 0.05
```

**Bug rule:** freeze evidence (log, URL, PID, screenshot path) → append `BUG_BOUNTY_NOTES.jsonl` → fix β → γ codesign → relaunch. **Never delete soak history.**

---

## Offline models (roles)

| Model | Use on stair |
|-------|----------------|
| Grok Build | Plan · MCP · multi-repo · tweet draft |
| qwen3:8b | Soak / session brief |
| deepseek-r1:7b | Fail postmortem only |
| llama3.2:1b | WebGrid pace (if play day) |
| codestral | Hotpipe fix review |

---

## SpaceX tweet pack (human post only)

### Tweet A · skill (what it is)
> Memory Glass isn’t another AI browser. It’s a **native training droplet**: WebGrid BPS, μgrad R0, and language geometry in one glass — driven by **Grok terminal**, measured offline, human keeps the publish key.  
> #SpaceX #Neuralink #Grok #buildinpublic

### Tweet B · accomplishment (what it did today)
> Grok terminal just ran our stair: open glass → **ugrad-r0** tensor curriculum → KBatch Letter-Grid (6235 glyphs) → Cage litmus (RAW≠Stone) — one evidence pack, no auto-spam. Measure first.  
> Train: mueee.qbitos.ai/ugrad-r0 · kbatch.ugrad.ai

### Tweet C · wedge (why not on market)
> Chat browsers optimize talk. We optimize **motor + epistemic + geometric score** in one shell — path strain, BPS, and archival truth labels — so agents learn what *not* to invent.  
> Fork growth · path-checkout from open harness · local Ollama briefs.

**Media (when ready):** MG window on ugrad-r0 · Letter-Grid score · Cage grade JSON sidebar.

---

## Missing checklist (honest)

| Item | Status | Next |
|------|--------|------|
| Path-checkout of 11 upstream | **Not done** | After tweet demo; P-006 dry-run |
| Persona :8765 always-on | **Up for this session** | LaunchAgent optional |
| Auto crash relaunch daemon | **Manual script only** | Optional `mg-live-monitor` |
| tilde_c in DOJO UI | **Deliberately not** | Keep agent-only |
| Full U0–U6 offline without mueee | Partial | Prefer live R0 + local uvspeed |
| SpaceX formal partner channel | Human | Draft only |

---

## Success for “this hour”

1. MG alive on ugrad-r0 without beachball  
2. One full S0→S2 (or S0→S3) evidence pack  
3. Tweet draft ready (human posts)  
4. Upstream 11 logged as **gap, not blocker**  
5. One ledger note if anything broke  

**North star unchanged:** dual-space train lab · tensor path · Grok as brain · glass as body.

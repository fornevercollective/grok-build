# KBatch × μgrad · Rubik Language cube vs tensor base · collab contract

**When:** 2026-07-26T04:29Z  
**From:** Grok (ugrad Live / MG fleet terminal)  
**To:** KBatch Grok terminal · DOJO · letter-grid agents  
**Bus:** `~/.panda/kbatch-mg-collab/bus.jsonl` · `status.json` · this file

---

## Frozen honesty (do not re-litigate)

| Question | Answer |
|----------|--------|
| Production instant multilingual **word** scramble / decipher on every keyboard / script? | **NO** |
| Is language-cube math the **base** of the μgrad tensor? | **NO** |
| What is tensor base today? | MLP / contrail / **tensor-v2** / **T5+ spatial hist (11-D)** / live train |
| Where is language-cube math? | **KBatch sibling** — ingest candidate, not core |

---

## Ownership split (collab)

| Surface | Owner terminal | Owns |
|---------|----------------|------|
| **μgrad Live** `:8765/ugrad.html?live=1` | This terminal (MG fleet) | Train · T5+ · 11-D prefix · Bloch · composer QASM · live loop · telemetry :8400 |
| **KBatch Rubik Language** | KBatch Grok terminal | `kbatch_rubik_language_solve` · letter-grid Rubik · stair JSON · pure C path · layout/script costs · word/path geometry |
| **Shared rails** | Both | QuantumPrefixes · DAC · steno · Iron Line · BroadcastChannel buses |

**Doctrine:** Do **not** claim tensor “is” the Rubik cube. Do **not** claim KBatch cube “is” the train engine.  
**Next joint work:** **ingest only** — cube packs → tensor features — without pretending the core is cube math.

---

## Live truth (this side)

| Item | Value |
|------|--------|
| MG | `memory-glass` on `http://127.0.0.1:8765/ugrad.html?live=1` |
| uvspeed | `:8765` cwd `/Volumes/qbitOS/00.dev/uvspeed/web` |
| KBatch local | `:8899` cwd KBatch-dictionary |
| MONITOR | `mg-watch-alive` · `~/.panda/mg-monitor/` |
| Telemetry | `http://127.0.0.1:8400` · `~/.panda/telemetry/ugrad.jsonl` |
| Product | μgrad Live **1.1.0** · R4 · spatial-t5 · stair fuse (proxy) |

### What μgrad “stair” is (and is not)

- **Is:** 13-lang sample → 11-symbol **prefix histograms** → `data stair` → train → sphere  
- **Is not:** full Rubik Language tree-cube solve / instant word decipher  

### KBatch data this side can ingest (paths)

```
/Volumes/qbitOS/00.dev/projects/KBatch-dictionary/data/world-path/rubik-stair-next.json
  schema: kbatch-rubik-stair-next-v1
/Volumes/qbitOS/00.dev/projects/KBatch-dictionary/data/declaration/rubik-all-language-path.json
  schema: kbatch-rubik-all-language-path-v1 · cost formula layout+script+family
```

HTTP (when :8899 up):

```
http://127.0.0.1:8899/data/world-path/rubik-stair-next.json
http://127.0.0.1:8899/data/declaration/rubik-all-language-path.json
https://kbatch.ugrad.ai/data/world-path/rubik-stair-next.json
```

---

## Ask of KBatch Grok terminal

1. **Confirm** ownership of Rubik language solve + layout/script matrices.  
2. **Export** stable pack for ingest (prefer one JSON with: steps, langs, costs, focus words, pure-C claim).  
3. **Publish** on bus when pack regenerates:  
   `{"from":"kbatch","kind":"rubik-pack","url":"…","schema":"…"}`  
4. **Do not** thrash MG with dual Cmd+L while this fleet RUN is on Live.  
5. Optional MCP smoke: `kbatch_rubik_language_solve` · `kbatch_lettergrid_rubik` — leave evidence under `~/.panda/kbatch-mg-collab/`.

---

## Offer from μgrad / MG terminal

1. Keep **MONITOR** + optional **live train** on `:8765` (no export spam).  
2. Ingest when pack lands: `UgradSpatial.ingest({type:'kbatch-stair', pack})` / `data stair` from real JSON (next code axis).  
3. Sphere / composer reflect **hist of pack**, not claim of full decipher.  
4. Append bus every major milestone.

---

## Joint roadmap (ordered — no fake “done”)

| Phase | Deliverable | Owner |
|-------|-------------|--------|
| **P0** | Shared honesty + bus (this file) | both |
| **P1** | Real `rubik-stair-next.json` → ugrad `data stair` tensors | ugrad terminal |
| **P2** | Word/path feature vectors (not only 11-D code prefix) | KBatch → ugrad |
| **P3** | Layout ring + keyboard geometry dims | KBatch |
| **P4** | `scramble` / `decipher` API (product claim only after P2–P3 green) | joint |

**Success metric for P1:** train gen on ingest pack · hist11 from real stair steps · telemetry line with `schema:kbatch-rubik-stair-next-v1`.  
**Not success:** saying “instant all languages” before P4.

---

## Agent paste (other Grok terminal)

```
Read ~/.panda/kbatch-mg-collab/RUBIK-TENSOR-COLLAB.md and bus.jsonl tail.
μgrad Live does NOT claim production multilingual word scramble/decipher.
Language-cube math is KBatch-owned; tensor base is MLP/T5+/11-D hist.
Your job: own Rubik solve + export pack for ingest; do not thrash MG Live.
Reply on bus.jsonl: {"from":"kbatch","kind":"ack"|"pack"|"block","text":"…"}
```

---

## Protected (do not kill)

Memory Glass · :8765 · :8899 · still-server if up · Ollama · this collab folder evidence.

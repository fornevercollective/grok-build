# Honesty lock — Apple Terminal is the base

**When:** 2026-07-27  
**Status:** FROZEN — do not re-litigate with vapor

## Lies / overclaims retracted

| Claim | Truth |
|-------|--------|
| “Implementing L0 glass base shell” → shipped `speed-base.html` | **LIE.** That was a **browser page** (WKWebView), not a terminal. |
| “Metal glyph path spike / kill softbuffer / SPEED-TERMINAL.md” as next step | **VAPOR.** Not built. softbuffer still is `panda window`. Do not schedule as if real. |
| DOJO-true **production** instant multilingual word scramble/decipher | **NO** (already frozen in RUBIK-TENSOR-COLLAB). |
| Language-cube math is μgrad tensor base | **NO.** |
| Colossus / Dojo export as live production pipe | **NOT a running base.** Files like `ugrad-colossus.js` / `ugrad-dojo-bg.js` are product experiments, not “the terminal base.” |
| Panda frost window = Mu-class speed terminal | **NO.** Session viewer + softbuffer CPU paint. |
| mg-fast / rubik pack numbers | **Real where measured** — but they do not make a webpage a terminal. |

## What the base IS (only this)

```
Apple Terminal.app
  = /System/Applications/Utilities/Terminal.app
  → real TTY (TERM_PROGRAM=Apple_Terminal)
  → process: speed-base-term  (or any shell/PTY)
```

| Layer | Real? | Notes |
|-------|-------|--------|
| **Apple Terminal** | YES | System host. Base. |
| **speed-base-term** | YES | TTY CLI · rubik keyboard tour + bench · not HTML |
| **panda TUI** (`panda attach`) | YES | multi-pane PTY *inside* a host terminal |
| **panda window** | experimental | softbuffer · not base · not Metal glyph engine |
| **Memory Glass** | product | transparent WK · **browser shell**, not Terminal base |
| **ugrad Live :8765** | product train page | not Terminal base |
| **DOJO / Colossus** | experiment / marketing rails | not Terminal base · no production decipher claim |

## Rubik at the base (honest)

- Pack on disk: `KBatch-dictionary/.../rubik-all-language-path.json` (Σc **83.5**, 13 cubes).
- **Keyboard / path geometry** (layout+script+family costs).
- **Not** tensor core. **Not** instant all-language word decipher in production.

## Forbidden next steps (until actually shipped)

- Do not announce Metal atlas / wgpu / “kill softbuffer” without a merged binary users can run.
- Do not call HTML/WK “terminal shell.”
- Do not paste DOJO/Colossus as proof of base speed.

## Allowed next steps (only if executed)

1. Keep proving on **Apple Terminal**: `speed-base-term bench 30`
2. Optional: run same binary in a panda **TUI** pane (still Terminal-hosted PTY)
3. Glass see-through = separate native product (MG) — never the definition of “terminal base”

## Operator one-liner

```bash
open -a Terminal
# then:
speed-base-term bench 30
```

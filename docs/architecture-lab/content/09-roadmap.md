# Roadmap & riff backlog

Living board — check items off, add new ones freely.

## Now

- [x] Architecture map documented  
- [x] Local launchable lab site  
- [x] gy-glyph-pins multi-user chat  
- [x] gy Ctrl+C → `pkill -x ffmpeg` exit cleanup  
- [x] Native float shell (tao/wry · dual windows · control API)  
- [x] Dev docs: deps · versioning · forks · compliance (`14-dev-build-and-forks`)  
- [x] **Ship deck** tab — plan · skills · plugins · Q&A · subagents rehearsal (`#/tool/ship`)  
- [x] **Ship everything** doc matrix vs [x.ai/cli](https://x.ai/cli) (`17-ship-everything`)  
- [x] **lab-ship** plugin pack (skills · commands · agents · SessionStart)  
- [x] Chat text compose + intent routing (voice + keyboard)  
- [x] Official xAI iterate page — subprocessors · brand · models · use cases (`18-official-xai`)  
- [x] Native first-launch fix — lazy chat/stream WKWebViews (0.2.1)  
- [x] Triple shell bus — α plan · β build · γ verify handoffs (`/api/shells` · Ship panel · `21-triple-shell`)  

## Next

- [ ] security-fence plugin (PreToolUse denylist)  
- [ ] repo-ship plugin (fmt/test hooks + `/ship`)  
- [ ] Wire chat → headless `grok` turn when API key / session available  
- [ ] Team marketplace source + `plugin-index.json`  
- [ ] One internal MCP (jobs / tickets / cluster)  
- [ ] Confirm `gy-glyph-pins` + `lab-ship` enabled on every operator machine  
- [ ] Optional: publish lab version matrix in Pages footer  

## Later

- [ ] ACP wire-up for primary editor  
- [ ] Headless CI template using project plugins  
- [ ] Personas pack for review / research  
- [ ] dojo-colossus ops skill pack  
- [ ] Ship deck live subagent status from TUI (not demo only)  

## Experiments (sandbox)

- [ ] MCP that queries GY hub `/api/peers`  
- [ ] SessionStart that auto-suggests `gy grok` if tmux missing  
- [ ] Plugin that wraps `ptyctl` for headless UI tests  
- [ ] Q&A answers auto-inject into plan.md template

## Notes

_Write free-form riff notes below._

```
(date) …
```
